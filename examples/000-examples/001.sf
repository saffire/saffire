/*
 * Simple language lexing test
 */

// Use statements
use fooFramework as foo;
use BarFramework as bar;
use io;
use test;
use tmp as aliased;

import io from io;
import io;
import io as io from io;
import foo as bar from baz;

/* Output:
*/
