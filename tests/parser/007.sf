/*
 * Conditionals
 */
if ($a == 1) {
    $a += 1;
    $b -= 1;
} else {
    $a -= 1;
    $b += 1;
}

// Conditional with single statements, without curly brackets
if ($a >= 1) $b+=1;
if ($a != 1) $b+=1; else $c+=1;

// Without parentheses
if $foo $a+=1;

/* Output:
*/
