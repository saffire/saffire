import WebServer\WebServer;
import io;

host = saffire.args(1) ?? "127.0.0.1";
port = saffire.args(2) ?? 4000;
docroot = saffire.args(3) ?? "docroot";
index = saffire.args(4) ?? "index.html";

WebServer(host, port, docroot, index).run();

